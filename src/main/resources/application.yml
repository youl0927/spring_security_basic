spring:
  datasource:
    url: jdbc:postgresql://192.168.0.8:5432/book
    username: mi_youl_admin
    password: 
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        use_sql_comments: true

logging.level:
  org.hibernate.SQL: debug
  #  org.hibernate.type: trace